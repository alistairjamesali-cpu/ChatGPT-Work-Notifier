# CODEX — EXECUTE FIRST (v2.4.0)

This is the authoritative in-place v2.4.0 source root. It is Chrome-only and manages one captured Chrome window in the user's existing profile. Never create/copy a Chrome profile, control ordinary Chrome windows, kill `chrome.exe`, add unsafe browser switches, call ChatGPT network APIs, or restore the removed Windows-app watcher.

For source finalisation, run `Maintenance\RUN_CODEX_FINAL_WINDOWS_VALIDATION.bat`, repair any evidenced failure, regenerate `FILE_SHA256SUMS.txt`, and run the complete source gate twice from this same root. The v2.4.0 gate includes the isolated live bridge contract, 123-item repair matrix, all three supplied DOM fixtures, actual current-task selector tests, security/static/material audits, and Windows PowerShell parsing.

Historical v2.3.0 Stage-1 runtime reports remain evidence of the version actually exercised; do not relabel them as v2.4.0. Live Windows repetition is optional for this implementation handoff and must not be fabricated. Do not create a replacement application ZIP or duplicate release root unless the user separately asks.
